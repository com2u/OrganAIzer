# File: /home/com2u/src/OrganAIzer/hasura/metadata/databases/default/tables/public_status.yaml
# Purpose: Hasura configuration for the Status table

table:
  name: status
  schema: public

# Array relationships
array_relationships:
  - name: entries
    using:
      foreign_key_constraint_on:
        column: status
        table:
          name: entry
          schema: public

# Insert permissions
insert_permissions:
  - role: user
    permission:
      check: {}
      columns:
        - status
        - description
        - color
        - icon

# Select permissions
select_permissions:
  - role: user
    permission:
      columns:
        - status
        - description
        - color
        - icon
      filter: {}

# Update permissions
update_permissions:
  - role: user
    permission:
      columns:
        - description
        - color
        - icon
      filter: {}
      check: {}

# Delete permissions
delete_permissions:
  - role: user
    permission:
      filter: {}
